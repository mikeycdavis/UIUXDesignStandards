# no-ui fixture

A repository with no user interface: a library and its tests. Nothing here is a UI signal, and
that absence is the point — the classifier must find zero signals over a complete scan before the
`no-ui` declaration in `project-policy.yml` can become NOT_APPLICABLE.

The words "component", "stylesheet", and "viewport" appear in this sentence and establish nothing.
